---
title: "A Plan for the Rest of the Quarter!"
date: "November 10, 2022"
author: "Stat 331"
output:
  xaringan::moon_reader:
    lib_dir: libs
    css: ["xaringan-themer.css", "slide-style.css"]
    nature:
      highlightStyle: solarized-light
      highlightLines: true
      countIncrementalSlides: false
      ratio: "16:9"
      slideNumberFormat: |
        <div class="progress-bar-container">
          <div class="progress-bar" style="width: calc(%current% / %total% * 100%);">
          </div>
        </div>
---


```{r, echo = FALSE, message = FALSE, warning = FALSE, fig.width = 8, fig.height = 6}
knitr::opts_chunk$set(fig.align = "center", 
                      message = FALSE, 
                      warning = FALSE)

options(htmltools.dir.version = FALSE)

library(xaringanthemer)
library(xaringan)
library(tidyverse)
library(flair)
library(palmerpenguins)

style_duo_accent(
  primary_color      = "#0F4C81", # pantone classic blue
  secondary_color    = "#B6CADA", # pantone baby blue
  header_font_google = google_font("Raleway"),
  text_font_google   = google_font("Raleway", "300", "300i"),
  code_font_google   = google_font("Source Code Pro"),
  text_font_size     = "30px"
)

fish <- read_csv(here::here("7_functions", "Lab", "BlackfootFish.csv"))
```

class: center, middle

.larger[What the next two weeks look like...]

---

.larger[Learning Targets]

**Weeks 9 & 10**

- *DSM-1*: I can simulate data from a variety of probability models.

- *DSM-2*: I can fit a linear regression and extract necessary summary measures.

- *DVS-6*: I can create tables which make my summaries clear to the reader.

- *DVS-7*: I show creativity in my tables.

---

.larger[How your supporting artifacts should look...]

.pull-left[
**Goal:** Demonstration of targets in Lab Assignment(s) and / or Challenge(s)
]
.pull-right[
**Notes:** I added WD-7 (pivoting) this morning & removed DSM-3 (simulation for regression)
]
---

.larger[Revisions]

The final round of revisions will be accepted on **Sunday, November 27**. 

You can revise any Lab Assignment or Challenge one additional time before
including it in the portfolio.

- You **do not** need to submit the revised Lab / Challenge to Canvas
- You **do** need to indicate what you are presenting is an
additional revision done for the portfolio
- You **will not** receive feedback on revisions done for the portfolio

---

class: center, middle, inverse

.huge-text[Lab 7 Recap]

---

class: center, middle, inverse

.huge-text[How to get side-by-side plots?]

---

.bitlarger[Getting Creative...]

.pull-left[
```{r, echo = FALSE, out.width = "80%"}
fish %>% 
  filter(is.na(weight)) %>% 
  group_by(year, section, trip) %>% 
  count() %>% 
  mutate(trip = as.factor(trip), 
         year = as.factor(year)) %>% 
  ggplot(aes(y = year, x = n, fill = trip)) + 
  geom_col() + 
  facet_wrap(vars(section)) + 
  labs(y = "", 
       x = "") +
  scale_fill_brewer(palette = "Paired") +
  labs(title = str_c("Missing Fish Weights for ",
      "<span style = 'color:#A6CEE3'>**Trip 1**</span>",
      " and <span style = 'color:#1F87B4'>**Trip 2**</span>"),
      subtitle = "by Section of Blackfoot River"
    ) + 
  theme(plot.title = ggtext::element_markdown(size = 24),
        plot.subtitle = ggtext::element_markdown(size = 18),
        legend.position = "none", 
        axis.text.y = element_text(size = 12), 
        axis.title = element_text(size = 16)
        )

```
]

.pull-right[
<center>

**Some Options**

rotating your axes!

making sure people don't tilt their head!

using non-default colors!

changing the font and / or font size!

**adding your legend to your title!**
]

---

.bitlarger[Legend Colors in Plot Title]

```{r, eval = FALSE}
labs(title = str_c("Missing Fish Weights for ",
      "<span style = 'color:#A6CEE3'>**Trip 1**</span>",
      " and <span style = 'color:#1F87B4'>**Trip 2**</span>"),
      subtitle = "by Section of Blackfoot River"
    )
```

--

<center>
.large[How did I figure out the colors???]
</center>

--

```{r, eval = FALSE}
library(RColorBrewer)
brewer.pal(n = 2, name = "Paired")
```

```
[1] "#A6CEE3" "#1F78B4"
```

---

class: center, middle, inverse

.larger[Back to the 12 Days of Christmas...]

---

class: center, middle, inverse

.huge-text[What is `pmap()` doing?]

---

![](images/pmap.png)

---

class: center, middle, inverse

.huge-text[Where are we going today?]

---

.bitlarger[Step 1 -- Write a `sing_day()` function]

--

.bitlarger[Step 2 -- Iterate your `sing_day()` function over a set of days]

--

.bitlarger[Step 3 -- Style you output!]


